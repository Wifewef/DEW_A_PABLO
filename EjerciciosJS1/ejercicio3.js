/* Anida dos bucles for y muestra por la consola el resultado de los dos índices en cada iteración. Realiza 10 iteraciones en cada bucle. */
for (let i = 0; i <= 10; i++) {

    for (let j = 0; j <= 10; j++) {
        console.log(i + " - " + j);
    }

}